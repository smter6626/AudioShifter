# AudioShifter macOS 可运行 MVP 测试报告

## 1. 结论

**最终状态：PASS**

- 测试日期：2026-08-03（America/Phoenix）
- 范围：纯逻辑、文件系统、依赖解析、可取消进程层、真实 FFmpeg/Rubber Band 管线、中文 Tkinter GUI、computer-use 实际 UI 交互。
- 自动化结果：`130 passed`，失败 `0`，跳过 `0`。
- UI 结果：真实中文窗口、系统文件选择器、处理状态、成功/冲突/错误/取消/关闭交互全部通过。
- 排除范围：未执行 PyInstaller，未生成或验证 `.app`、`.dmg`，未处理签名、公证和正式分发。

## 2. 环境版本

| 组件 | 实测版本 |
|---|---|
| Python | 3.11.15，原生 arm64 |
| pytest | 8.4.2 |
| Tcl/Tk | 8.6.18，Aqua |
| FFmpeg | 8.1.2 |
| FFprobe | 8.1.2 |
| Rubber Band | 4.0.0 |

完整测试命令：

```bash
macos/.venv/bin/python -m pytest
```

分层结果：

| 测试层 | 数量 | 通过 | 失败 | 跳过 |
|---|---:|---:|---:|---:|
| 单元/文件系统/控制层故障注入 | 102 | 102 | 0 | 0 |
| 真实管线/真实窗口集成测试 | 28 | 28 | 0 | 0 |
| 合计 | 130 | 130 | 0 | 0 |

## 3. 四种输入格式与真实管线

所有素材均在 pytest 或 UI 自检的系统临时目录动态生成，测试结束后清理，未提交仓库。

| 格式 | 代表性输入名/特征 | 三阶段处理 | 输出探测 |
|---|---|---|---|
| MP3 | `song.final.v2.MP3`，大写扩展名、多点 stem | PASS | MP3 / 320000 bps / 44100 Hz / 2 ch |
| M4A | `quote'file.M4A`，大写扩展名、单引号 | PASS | MP3 / 320000 bps / 44100 Hz / 2 ch |
| WAV | `中文 音频 (test) & sample.WAV`，中文、空格、括号、`&`、48 kHz 单声道 | PASS | MP3 / 320000 bps / 44100 Hz / 2 ch |
| FLAC | `audio.FLAC`，大写扩展名 | PASS | MP3 / 320000 bps / 44100 Hz / 2 ch |

真实管线顺序已由阶段事件断言为 FFmpeg 解码/标准化 → Rubber Band → FFmpeg 编码 → FFprobe 验证。中间 WAV 实测为 `pcm_s16le`、44.1 kHz、双声道。`pitch=0/speed=0`、只变调、只变速、同时变调变速、变调 `-24/+24`、速度 `-95/+400` 均完成真实处理；440 Hz 合成输入经 `+3/-3` 后的自动频率估计分别验证了升调和降调方向。

速度方向测试：`-20` 的输出时长大于输入，`+20` 的输出时长小于输入；未设置缺乏音质证据的严格误差阈值。

## 4. computer-use GUI 自检

使用已安装且已授权的 computer-use 插件操作实际 Tkinter/Aqua 窗口；未安装插件、未更改插件配置、未申请权限、未修改 macOS 安全设置。

完整成功路径：

1. 通过固定命令启动 `AudioShifter 音频变调变速` 中文窗口。
2. 系统文件选择器只用于选择本轮 4 秒合成 WAV。
3. 输入变调 `+3`、变速 `-20` 后启动。
4. 在任务运行中观察到“正在变调和变速…”；窗口可继续读取和操作，开始按钮禁用、取消按钮可用。
5. 成功弹窗显示完整路径 `/Users/smterpro/Downloads/UI 自检 (合成) & MO0nCc4iOj+3-20%.mp3`。
6. 第二次相同参数运行先显示“不覆盖”与实际 `_2` 路径；确认后成功生成 `_2`。
7. 两份输出 SHA-256 均为 `bb75a8e25d053da374bb6810f3f572a913375b3392dbe596b7fa39d421d8e3e0`，证明第一份未被第二次覆盖或改写。
8. 源 WAV 处理前后 SHA-256 均为 `5ef902514bf7dbdb812b78031eb542d1139ec616a30b33b7865840ee073a582d`。

代表性首份输出 FFprobe：

```text
codec_name=mp3
sample_rate=44100
channels=2
stream_bit_rate=320000
duration=5.000000
size=202754
format_bit_rate=324406
```

输入为 4.000000 秒、48 kHz、单声道 PCM WAV；`-20` 对应 tempo `0.8`，输出 5.000000 秒，时长方向和换算一致。

错误和控制场景：

- 未选择输入：PASS，提示先选择四种支持格式。
- 变调小数 `1.5`：PASS，启动前拒绝。
- 变调越界 `+25`：PASS，启动前拒绝。
- 变速带 `%`：PASS，说明不要输入 `%`。
- 变速越界 `+401`：PASS，启动前拒绝。
- 重复启动：PASS，开始按钮禁用；运行时只观察到一个 Rubber Band PID，控制层直接二次调用测试也拒绝。
- 取消：PASS。120 秒合成输入以 `-95` 启动后立即取消，GUI 显示正常取消；无输出、无 FFmpeg/Rubber Band 遗留进程、无任务临时目录，源哈希不变。
- 运行中关闭选择继续：PASS，关闭取消且任务继续显示处理状态。
- 运行中关闭确认退出：PASS，任务取消、进程回收、临时目录清理后窗口退出，无残缺输出。

一次 20 秒取消尝试在操作到达前已正常完成，因此未计为取消证据；其输出和所有其他 UI 合成输入/输出均按完整路径确认后删除。可靠取消结论来自随后 120 秒输入的实际取消测试。

## 5. 命名、不覆盖、源保护与清理

- 固定双参数命名、显式正负号、零值、Decimal 尾零规范化：PASS。
- 基础名、`_2`、`_3` 分配：PASS。
- 初始同名文件和分配后的外部竞态均不覆盖：PASS。
- 最终发布使用排他创建；FFmpeg 先在任务工作区编码，不使用 `-y` 覆盖 Downloads 文件：PASS。
- 四种输入的处理前后源 SHA-256 不变：PASS。
- 成功、无效媒体、Rubber Band 失败、FFmpeg 解码/编码失败、最终探测失败和取消后的临时清理：PASS。
- 子进程取消使用独立进程组；父子进程均回收：PASS。
- `ENOSPC`、`EACCES` 与清理失败警告已通过确定性故障注入验证；无残缺目标，清理警告不伪装为主任务失败。

## 6. 覆盖的验证矩阵

自动化或实际 GUI 证据覆盖：

- `IN-T001`～`IN-T016`
- `PITCH-T001`～`PITCH-T015`
- `SPEED-T001`～`SPEED-T018`
- `NAME-T001`～`NAME-T015`
- `OUT-T001`～`OUT-T008`（物理磁盘实际写满未执行，`OUT-T006` 使用 ENOSPC 故障注入）
- `PIPE-T001`～`PIPE-T012`
- `DEP-T001`～`DEP-T009`
- `TEMP-T001`～`TEMP-T010`
- `TASK-T001`～`TASK-T006`、`TASK-T008`～`TASK-T014`
- `ERR-T001`～`ERR-T010`（真实磁盘满提示由错误模型与故障注入验证）
- `PRIV-T001`～`PRIV-T004`
- `EXCL-T001`～`EXCL-T004`
- `GUI-T001`～`GUI-T012`

`TASK-T014` 使用明确忽略 SIGTERM 的父子进程完成强制终止和回收测试。`TASK-T007`（恰好在真实 FFmpeg 编码时间窗点击取消）未单独以 GUI 时序命中；同一进程组取消器、残缺输出删除和编码故障清理分别已有自动化证据。`PKG-*` 属于下一 active step，本轮未执行。

## 7. 失败、跳过、未覆盖和已知限制

- 自动化失败：0。
- 自动化跳过：0。
- 未执行真实物理磁盘写满；使用 `ENOSPC` 注入验证用户错误、无残缺文件和清理语义。
- 未破坏真实 Downloads 权限；使用文件系统模拟与 `EACCES` 注入验证。
- 未做主观音质评分、长期压力测试或最低 macOS 版本实机矩阵。
- 当前源码运行依赖已验证的 Homebrew/虚拟环境；独立应用包、动态库重定位和脱离开发环境运行属于下一阶段。
- 许可证分发路线仍需在正式二进制分发前解决，不影响本地 MVP 行为验证。

## 8. 仓库与范围检查

- 测试音频、Downloads 输出、虚拟环境、缓存、第三方二进制均未提交。
- Windows 目录未修改，Windows 二进制未执行。
- 无机器码、激活码、授权文件或遥测逻辑进入 macOS 源码。
- 外部命令全部使用参数数组、`shell=False`；GUI 不构造 FFmpeg/Rubber Band 命令。
- 未执行 PyInstaller；未生成 `.app`、`.dmg`、`build/` 或 `dist/` 产物。
